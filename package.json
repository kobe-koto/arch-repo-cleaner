{
  "name": "arch-repo-cleaner",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "bundle": "esbuild index.ts --bundle --platform=node --outfile=dist/main.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.27.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "chalk": "^5.6.2"
  }
}
